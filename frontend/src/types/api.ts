/**
 * Frontend types — re-exported from the shared package so both sides
 * always share one contract. Frontend-specific helpers live below.
 */
export type {
  AdminBooking,
  AdminLoginResponse,
  AdminMeResponse,
  BookingStatusLabel,
  CreateBookingRequest,
  PaginatedBookings,
  PublicBooking,
  UpdateBookingStatusRequest,
} from '@shared/types'
